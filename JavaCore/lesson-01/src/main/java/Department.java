// như 1 bản vẽ trong đời thật(bản thiết kế )
public class Department {
    // 1. Thuộc Tính (Property)
    int id;
    String name;

    // 2. Phương thức (Method)
    void showInfo(){
        System.out.println("Department[" + id + "," + name + "]");
    }

}
